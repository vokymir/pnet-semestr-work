@page "/ptacci"
@inject BirdApiClient BAC
@inject Blazored.LocalStorage.ILocalStorageService LocalStorage

<PageTitle>Ptáčci</PageTitle>

<h1 class="mb-4">Ptáčci</h1>
<HelpBox InitiallyVisible="true">
Seznam všech ptáčků v aplikaci. Jestli zrovna ten tvůj chybí, přidej ho! Ale pozor - prosím, nepřidávejte ptáčky, kteří už existují. Byl by to zbytečný zmatek...
</HelpBox>

<div class="d-flex justify-content-between align-items-center mb-4">
    <Button Type="ButtonType.Link" To="/ptacek/novy" Color="ButtonColor.Primary">
        ➕ Přidat ptáčka
    </Button>
</div>

@if (B is null || !B.Any())
{
    <div class="alert alert-info">
        Nebyl nalezen žádný ptáček.
    </div>
}
else
{
    <div class="row">
        @foreach (var b in B)
        {
            <div class="col-12 col-sm-6 col-md-4 col-lg-3 mb-4">
                <Card class="h-100 shadow-sm border-0">
                    <CardBody>
                        <CardTitle class="fw-bold fs-5 mb-2">@b.BinomicName</CardTitle>
                        <div class="text-muted fst-italic mb-3">@b.FullName</div>

                        <Button Color="ButtonColor.Primary" To=@($"/ptacek/{b.Id}") Type="ButtonType.Link" class="w-100">
                            Zobrazit detail
                        </Button>
                    </CardBody>
                </Card>
            </div>
        }
    </div>
}

@code {
    private UserDto? U = null;
    private ICollection<BirdDto>? B = null;
    private List<ToastMessage> messages = new();

    protected override async Task OnInitializedAsync()
    {
        try
        {
            U = await LocalStorage.GetItemAsync<UserDto>("userDto");
            if (U is null) throw new Exception("Nelze načíst userDto z local storage.");

            B = await BAC.Bird_GetAllAsync();
        }
        catch (Exception ex)
        {
            messages.Add(new ToastMessage
            {
                Type = ToastType.Warning,
                Title = "Chyba",
                HelpText = $"{DateTime.Now}",
                Message = $"Problém s načítáním ptáčků. Zkontrolujte, že jste přihlášeni.<br/>Problém: {ex.Message}",
                AutoHide = true
            });
        }
    }
}
